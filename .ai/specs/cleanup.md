Cleanup to do before public first release:
- remove all instances of "ian" - 41 references found across 14 files on July 16, 2026 - not everyones name will be "ian" who uses this cortex repo
- clean up top level readme - it reads like an in progress repo. once we are actually ready for release and are done, it should seem as such.
- clean up (delete) legacy docs that are no longer useful or in scope for what this repo is (wrc os stuff, tui stuff, etc) - if we ever need it back, it lives in git.
- .ai/ANTI-PATTERNS.md seems to be highly tailored for this repo but also my (ian) specific workflow. is this good or bad? when a new user installs cortex, will it be tailored for "ian"? I think I need help to understand what files in this repo are for local development and my personal agents to consume vs what files will be for any contributors in the repo, vs what files will be for simple consumers of the repo. in fact, most of the .ai/ files seem to be specific to me and my repo and my inner workings of my brain and development patterns and software engineering workflows. can you explain to me how someone else would use this cortex repo? would agents know to create the same exact type of documentation files like work.md and context.md or what? is this a missing package that could live alongside brickwall for cortex specific files? I know a lot of developers that do not create any custom ai files other than plans that do not get committed to the repo and a claude.md - feels like a sub product to live in the cortex toolkit... i do see "cortex init" described but we should probably discuss that as well as all the other extractions in the extraction plan.
- on the AGENTS.md quality gates blurb, i would read the "Use within the development chain" blurb in .ai/specs/ians-brain.md and lets discuss - seems like the current instructions propagated throughout the repo may be too rigid for what we want to persist as the living functions.
- i noticed the "Config" section in packages/brickwall/README.md - this is pretty sweet! Is this current state? I do have a couple ideas for the roadmap: adding scss (and other style tags) to code extensions to furthur clean up the portfolio repo (perhaps it already works like this, not sure, I did see the // or { ".ts": 250, ".scss": 600 } comment). storyDirs could probably be renamed to docDirs or something more generic. as far as the budgets section is concerned, i think we need better naming. there is probably a difference between plan docs and doc docs, plus some people use html for docs instead of md, something we need to be cognizant of. I noticed we have a codeLines, storyLines, and mdLines, but only a filecount for mdFileCount. it makes sense that we wouldnt put a cap on code files, but it begs the question, are stories counted differently than md files? this is confusing.
- Changelog (pre-publish) doesnt seem like something that should simultaneously live in packages/brickwall/README.md - as the repo scales, this changelog section will grow, as it should, but the file being a checked document readme has a fixed limit, this means we either need to decide on a source of truth, like the changesets or something
- help me understand why we have packages/brickwall/src/index.ts - is this barrel file critical or is this just how pacakges are made? i can see a world where as we add more to the package, this barrel file will not pass its own code lines check...
- how will this package scale for a large repo, 10000 or even 100000 files? each from 1000 to 10000 lines long? are we thinking about the problem from these perspectives? I remember learning algorithms to walk trees and also loop through lists that could potentially be faster than linear loops. I also remember learning concurrent programming and multithreaded programming and distributed systems for load balancing and speed. there are hundereds of systems out there, not all of them are possible with a simple node.js/ts based repo. just some thoughts ive had while reading the code.
- it might be valuable to clean up the archive and ignore dirs right before publish (at least, the files we arent using because they are depricated, not actually the historical files to show agents why something was done or how or when).
- i am a little confused why docs/USAGE.md has information about Using brickwall. should that live in the packages/brickwall/README.md or something more close to the package itself? perhaps i do not understand how monorepo documentation works. we should discuss. I dont know if i have a preference, but I do wonder what will happen once we add another package alongside brickwall... also, i already see drift with this file, and packages/brickwall/README.md specifically with the cli instructions. feels like we are already falling into the trap that cortex is meant to prevent. i will say this might be a job best suited for the janitor custom agent...
- id like to include some research for how many files an agent in 2026 can ingest into its context without blowing up, as well as include any research about optimal file sizes for agents to handle both for code and for docs and for plans. i think there is research out there, but we should spawn up an investigator to find data in our favor to link somewhere in the repo.
- i already see a ton of .md files in this repo, spread across /, packages/brickwall, packages/brickwall/src, packages/brickwall/test, /docs/, and /.ai/ - when we run brickwall on the entire cortex repo, is it something that is run on the repo, and then each package individually, or on the entire repo, package agnostic? how would brickwall --all work here? I ask because many people use monorepos and if an agent is working in a sub repo instead of the top level repo, there might be different rules to follow, same deal applies to if an agent is working in the Sites/ directory where cortex is located, but as a real world example, if they are working on a api and miniapp and service repo, none of which are in a monorepo. could they all have brickwall or cortex installed? how would it know it was in a monorepo? what if it was in a monorepo but cortex was only configured at the top level instead of within each sub repo? What would that user experience look like? what about if that person was working on a ticket that spanned 5 repos and was using worktrees? im just trying to imagine the experience for various types of devs (who i actually have been (these examples are pulled from real live situations ive worked in))
- the mastering metaphor is a bit corny. we dont need the presets for soft, multiband, etc. tying it to music like that isnt exactly what i had in mind. it was more of an example when i was being interviewed with one of the initial planning agents. i will say, there will be tools in this repo, such as a documentation compressor that the janitor will use, or a build pipeline that the orchestration agent will use when managing subagents, or a validation workflow for documentation that the planning agents (and sub agents) will use. its a bit more whimsical than staying truly rigid with one domain like music. i would rather name something with an appropriate clear name than try to fit something neatly within a box.
- in terms of other ai files, such as skills, deterministic code workflows, and custom agents, these should appear naturally, not simply created for the sake of creating them. I do believe i still need help understanding how a custom agent can have its own skills that reference its own custom mcp, cli, or script based tools that do not drift into the short term memory of other agents who are spawned as a different custom agent or a generic agent. i believe it is possible to scope skill files to specific directories, so that if an agent is spawned in that directory it has access to the skill, but if the agent is spawned in a higher up directory, then it doesnt have the skill, however, something ive noticed is that if the agent spawned in a different directory gets access via crawling the filesystem or by the user directly pointing them to the file via progressive disclosure (intentionally or not), the agent has access to the skill and would use it. i think thats fine but it would be pretty neat to learn how to build these custom agents to be used by any orchestrator in any directory in a repo and each custom agent could actually have its own scoped skillset. this is important to me because if the orchestrator agent has too many skills, it always blows up the context due to the skill cache having too many files. it would be ideal if the orchestrator agent has its own skills that are unique to it (and that other agents, generic or subagents spawned by the orchestrator) and vice versa, if the specific subagent (such as a prompt engineer agent, a planner, an interviewer, a discovery agent, a research agent, a peer review documentation agent, a phase implementation agent, a full plan implementation agent, a peer review code agent, a documentation update agent, a janitor agent, a code review agent, a qa agent that leverages playwright or other tools, a retrospective agent (who uses completed plans, code review, and more, to push knowledge left so that future agents do not get stuck at the same thing), a handoff prompt agent, a server runner agent (aware of what to do to set up a repo for the first time or run commands to spin up and turn off a server for local or other kind of development) and more (i have many ideas for custom agents)) has its own scoped skills that elevate it beyond a simple model and effort selection done by the orchestrator. its a matrix of decisions, which subagent should the orchestrator spawn, using which model, using which level of effort? long term, it could be possible to ship a cortex package that leveraged iterm, tmux, and multiple frontier model providers (such as openai codex, anthropic claude, xAI grok, google deepmind, meta muse spark, mistral large, alibaba qwen, deepseek, moonshot, and imb bob, for example) to manage custom subagents across various products and configure effort and model within product. I have already done this manually using handoff prompts and ive seen it done using a custom plugin that allows claude to use codex, but i am thinking long term here. it is not good to be reliant on one provider.
- my friend jacob built a few tools that he has shared with me: /Users/ianrios/Downloads/jacob - he also has a public repo that i would personally use: https://github.com/jjacoblee/arborist - it would be cool to reference this somehow as part of the cortex toolkit. however its not built by me. its just something he made. what would it look like to collaborate? could it be possible to fork a version of arborist into cortex and repackage it in that way? i dont know how this open source stuff works 100% as a contributor. I am mostly a consumer.
- cortex branding: something to consider for first public release - im thinking an image of a brain made with ascii - we can use this on the top level readme as well as on install / init in the terminal - also could be fun to use the brain emoji or other things in the terminal as part of the processes that run
