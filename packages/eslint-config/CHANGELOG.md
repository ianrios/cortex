# @ianrios/eslint-config

Hand-maintained until the first Changesets release; generated entries
will be prepended above by `changeset version`.

## Unreleased (pre-publish)

- 2026-07-17 — First extraction (Phase 3): the generic strict core
  from petal's proven config (strictTypeChecked + stylisticTypeChecked
  + hard-line rules, projectService on); framework layers stay
  consumer-side.
