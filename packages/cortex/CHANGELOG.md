# @ianrios/cortex

Hand-maintained until the first Changesets release; generated entries
will be prepended above by `changeset version`.

## Unreleased (pre-publish)

- 2026-07-17 — First extraction (Phase 3): `cortex init` — per-file
  never-overwrite scaffold of the `.ai/` skeleton, AGENTS.md/CLAUDE.md
  pair, resolved brickwall config (drift-guarded against
  DEFAULT_CONFIG), and prettier/markdownlint/knip starters. Umbrella
  bin owns scaffolding only (ADR 0005).
