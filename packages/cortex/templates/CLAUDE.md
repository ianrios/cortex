# CLAUDE.md

Read `AGENTS.md` — it is the single entry point for all agents,
including Claude Code. Everything (startup order, operating model,
quality gates, context budgets) lives there and in `.ai/`.
