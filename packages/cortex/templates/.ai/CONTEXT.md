Type: <stack: language, package manager, runtime floor>
Purpose: <one sentence — what this repo produces and for whom>
Backend: <services touched, or "none">

Commands:

- Install: <command>
- Check: <command — the full gate chain>
- Test: <command>
- Run: <command>

Constraints:

- <hard constraints agents must never violate: versioning scheme,
  public API surface, no-network rules, deploy cautions>
