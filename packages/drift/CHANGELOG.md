# @ianrios/drift

Hand-maintained until the first Changesets release; generated entries
will be prepended above by `changeset version`.

## Unreleased (pre-publish)

- 2026-07-17 — First extraction (Phase 2): `runChecks` (typed key
  union, lazy checks, per-check error isolation) + `formatChecks`
  (human/`--json`, exit 0/1/2 with 2 = no valid verdict). Source
  pattern: the portfolio's validate.ts registry.
